package org.everest.flex.model
{
    /**
     * Keeps content type constants. At everest level this is only the atom
     * content type. In the everest application a content typ has to be defined
     * for each member and collection view to be able to define the mapping to
     * the module.
     *
     * @author rothe
     */
    public class ContentType
    {
        public static const APP_SERVICE:String = "application/atomsvc+xml";
    }
}